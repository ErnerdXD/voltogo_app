import 'package:flutter/material.dart';

/// Central place for app-wide color tokens.
class AppColors {
  AppColors._();

  static const Color seed = Color(0xFF43C5D4);
  static const Color appBarBackground = Color(0xFF091522);
  static const Color onAppBar = Colors.white;
}
